package com.baliyun;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import java.util.Date;

@RunWith(SpringRunner.class)
@SpringBootTest
public class SpringBootMybatisPlueApplicationTests {

/*
    @Autowired
    LandlordService landlordService;
    @Test
    public void contextLoads() {
        System.out.println("插入测试：");
        Landlord landlord = new Landlord();
        Integer x = 1234567890;
        landlord.setId((long)1);
        landlord.setUserStatus(0);
        landlord.setPhone("185989898585");
        landlord.setUsername("三丰");
        landlord.setUserPassword("123456");
        landlord.setUpdateDate(new Date());
        landlord.setRegisterDate(new Date());
        Boolean y = landlordService.insert(landlord);
        System.out.println(y);
    }

*/


}
